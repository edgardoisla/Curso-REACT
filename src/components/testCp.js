/* export const  = React.createContext();

const Provider = (props) => {
    // Initial Provider State
    const state = {};

    // Reducer Function
    const  = (state, action) => {
        
    };

    return (
        <.Provider value={{state: state, }}>
            {props.children}
        </.Provider>
    );
}

export default Provider;

*/